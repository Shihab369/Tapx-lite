
services:
  postgres:
    image: postgres:15-alpine
    container_name: tapx-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tapx
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: tapx-redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  pgdata:
